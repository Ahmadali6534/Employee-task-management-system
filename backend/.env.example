# SECURITY: This file must never be committed to git (confirm it's in .gitignore).
# Values below are for LOCAL DEVELOPMENT only — generate fresh, unique values
# for staging/production and inject them via your host's secret manager/env
# injection instead of a checked-in file.

DATABASE_URL=postgresql://postgres:CHANGE_ME_STRONG_LOCAL_DB_PASSWORD@localhost:5432/employee_task_db

# Generate a new one for every environment, e.g.:
#   python -c "import secrets; print(secrets.token_urlsafe(64))"
SECRET_KEY=fpbdld1f-iOc_-LQ1yvnjCKuGc_qgUfVlsk_bUYFTIcsrOWZ4SLyMgO9Y6xMtDrtkFHGF6eRPC6sBmOrcjkCxQ

ALGORITHM=HS256

ACCESS_TOKEN_EXPIRE_MINUTES=60

# development | production — controls Secure/SameSite cookie flags, etc.
ENVIRONMENT=development